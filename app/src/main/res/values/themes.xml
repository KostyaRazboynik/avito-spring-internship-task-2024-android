<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.KinopoiskQuickSearch" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/translucent_black_color</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/color_light_grey</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.KinopoiskQuickSearch" parent="Base.Theme.KinopoiskQuickSearch" />
</resources>